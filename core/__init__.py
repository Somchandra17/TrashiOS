"""TrashiOS core package — device bridge (IOSDevice), runtime bridge (FridaBridge),
shared config/state, the report engine, the AI-review packager, and PII detection.

Import submodules directly, e.g. ``from core.ios_device import IOSDevice``.
"""

__version__ = "1.0.0"
